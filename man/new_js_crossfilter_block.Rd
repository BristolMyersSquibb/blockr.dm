% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crossfilter-block.R
\name{new_js_crossfilter_block}
\alias{new_js_crossfilter_block}
\title{Legacy constructor for the crossfilter block (deprecated)}
\usage{
new_js_crossfilter_block(
  active_dims = list(),
  filters = list(),
  range_filters = list(),
  measure = NULL,
  agg_func = NULL,
  ...
)
}
\arguments{
\item{active_dims}{Named list of per-table active filter columns.
E.g., \code{list(adsl = c("SEX", "AGE"), adae = c("AESEV"))}}

\item{filters}{Named list of per-table categorical filters.
E.g., \code{list(adae = list(AESEV = c("SEVERE")))}}

\item{range_filters}{Named list of per-table range filters.
E.g., \code{list(adsl = list(AGE = c(40, 80)))}}

\item{measure}{Measure column as \code{"table.column"} string, or \code{NULL}
for row counts. E.g., \code{"orders.amount"}.}

\item{agg_func}{Aggregation function: \code{"sum"} or \code{"mean"}. Only used
when \code{measure} is set.}

\item{...}{Forwarded to \code{\link[blockr.core:new_transform_block]{blockr.core::new_transform_block()}}}
}
\value{
A crossfilter block carrying a \code{js_crossfilter_block} subclass.
}
\description{
\code{new_js_crossfilter_block()} is the pre-rename alias kept so saved boards
that reference the \code{js_crossfilter_block} UID continue to deserialize.
All new code should use \code{\link[=new_crossfilter_block]{new_crossfilter_block()}} directly. This wrapper
emits a \code{\link[=.Deprecated]{.Deprecated()}} warning at construction time and tags the
produced block with a \code{js_crossfilter_block} subclass so the served
Shiny UI renders a visible deprecation banner above the normal block
output.
}
