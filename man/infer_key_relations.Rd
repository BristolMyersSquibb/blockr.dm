% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dm-block.R
\name{infer_key_relations}
\alias{infer_key_relations}
\title{Infer PK/FK relationships from a named list of tables}
\usage{
infer_key_relations(all_tables)
}
\arguments{
\item{all_tables}{Named list of data frames}
}
\value{
A list with \code{pks} (each \code{list(table, column)}) and \code{fks} (each
\code{list(child_table, child_column, parent_table)})
}
\description{
For each column name shared by two or more tables: a table where the
column is complete and unique is a PK candidate, one where it is not is
an FK candidate. The first PK candidate wins and every FK candidate is
linked to it.
}
\keyword{internal}
