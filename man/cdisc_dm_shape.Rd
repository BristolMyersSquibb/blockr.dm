% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cdisc-dm-block.R
\name{cdisc_dm_shape}
\alias{cdisc_dm_shape}
\title{Everything the CDISC expression needs to know about the input dm}
\usage{
cdisc_dm_shape(dm_obj)
}
\arguments{
\item{dm_obj}{A dm object}
}
\value{
A list with \code{parent} (\code{NULL} when no CDISC parent was found),
\code{child_tables}, \code{fks}, \code{pk_tables} and \code{dedup_info}
}
\description{
Reduces a \code{dm} to the plain, comparable description the expression builder
reads from it: the parent table, the child tables carrying USUBJID, the
existing keys to strip, and the duplicated columns to drop. Deliberately
returns base vectors and lists (not the \verb{dm_get_all_*()} tibbles) so two
equal-but-freshly-built inputs produce an \code{identical()} result — that is
what lets the \code{reactiveVal} holding it treat a spurious re-derivation as a
no-op. See the note on the observer in \code{\link[=new_cdisc_dm_block]{new_cdisc_dm_block()}}.
}
\keyword{internal}
