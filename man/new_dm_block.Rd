% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dm-block.R
\name{new_dm_block}
\alias{new_dm_block}
\title{Create dm Block Constructor}
\usage{
new_dm_block(infer_keys = TRUE, ...)
}
\arguments{
\item{infer_keys}{Logical, whether to automatically infer primary and foreign
key relationships from columns with matching names. Default is \code{TRUE}.}

\item{...}{Forwarded to \code{\link[blockr.core:new_transform_block]{blockr.core::new_transform_block()}}}
}
\value{
A block object for creating dm objects
}
\description{
This block combines multiple data frames and/or dm objects into a single
dm (data model) object.
}
\details{
Inputs can be data frames or existing dm objects:
\itemize{
\item Data frames are added as new tables in the dm
\item Existing dm objects have their tables merged into the result
\item If table names conflict, later inputs overwrite earlier ones
}
}
\examples{
# Create a dm block
new_dm_block()

# Create a dm block with auto-inferred relationships
new_dm_block(infer_keys = TRUE)

if (interactive()) {
  library(blockr.core)
  library(blockr.dm)

  # Combine multiple tables into a dm
  serve(
    new_board(
      blocks = list(
        airlines = new_dataset_block(
          dataset = "airlines", package = "nycflights13"
        ),
        flights = new_dataset_block(
          dataset = "flights", package = "nycflights13"
        ),
        dm_obj = new_dm_block()
      ),
      links = c(
        new_link("airlines", "dm_obj", "1"),
        new_link("flights", "dm_obj", "2")
      )
    )
  )
}

}
