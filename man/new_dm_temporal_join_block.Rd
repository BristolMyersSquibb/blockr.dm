% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dm-temporal-join-block.R
\name{new_dm_temporal_join_block}
\alias{new_dm_temporal_join_block}
\title{dm Temporal Join Block Constructor}
\usage{
new_dm_temporal_join_block(
  left_table = "",
  left_date = "",
  right_table = "",
  right_date = "",
  window_days = 7,
  direction = "after",
  ...
)
}
\arguments{
\item{left_table}{Character, the name of the left table (e.g., "adae"). Default "".}

\item{left_date}{Character, the date column in left table (e.g., "ASTDT"). Default "".}

\item{right_table}{Character, the name of the right table (e.g., "adlb"). Default "".}

\item{right_date}{Character, the date column in right table (e.g., "ADT"). Default "".}

\item{window_days}{Numeric, the time window in days. Default 7.}

\item{direction}{Character, one of "after", "before", or "around". Default "after".}

\item{...}{Forwarded to \code{\link[blockr.core:new_transform_block]{blockr.core::new_transform_block()}}}
}
\value{
A block object that outputs a data frame with the temporal join result
}
\description{
This block creates a temporal join between two tables in a dm object.
It finds rows from the right table that fall within a time window of rows
in the left table, useful for causality assessment in clinical data.
}
\details{
Returns a data frame with the joined result.

The temporal join:
\enumerate{
\item Joins left and right tables by their common key (usually USUBJID)
\item Calculates days between left_date and right_date
\item Filters to rows within the window based on direction:
\itemize{
\item "after": 0 <= days_diff <= window_days (right after left)
\item "before": -window_days <= days_diff <= 0 (right before left)
\item "around": abs(days_diff) <= window_days (either direction)
}
\item Returns the result as a data frame
}
}
\examples{
# Find labs within 7 days after each adverse event
new_dm_temporal_join_block(
  left_table = "adae",
  left_date = "ASTDT",
  right_table = "adlb",
  right_date = "ADT",
  window_days = 7,
  direction = "after"
)

}
