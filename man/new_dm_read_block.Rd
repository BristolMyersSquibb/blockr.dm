% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dm-read-block.R
\name{new_dm_read_block}
\alias{new_dm_read_block}
\title{Read multiple files into a dm object}
\usage{
new_dm_read_block(
  path = character(),
  selected_tables = NULL,
  args = list(),
  ...
)
}
\arguments{
\item{path}{Character. Path to file or directory. Can be:
\itemize{
\item An Excel file (.xlsx, .xls): Each sheet becomes a dm table
\item A ZIP file (.zip): Extracted files become dm tables
\item A directory path: All data files in directory become dm tables
\item An RDS file (.rds): Can contain a dm, data.frame, or list of data.frames
\item An RData file (.rdata, .rda): All data.frames become dm tables
}}

\item{selected_tables}{Character vector naming the tables to read. \code{NULL}
(the default) means nothing has been chosen yet and the block waits:
pointing it at a directory of study data must not read all of it before
a table is asked for.}

\item{args}{Named list of reader options applied uniformly to every member
of the container: each member's format entry picks the parameters it
understands, so \code{args = list(sep = ";")} reads every CSV in a folder as
semicolon-separated and leaves the other formats untouched. Which
options a source offers is declared by its members' formats
(\code{\link[blockr.io:source_options]{blockr.io::source_options()}}) and the block's gear band is generated
from that, so a source whose formats declare nothing shows no gear at
all. When \code{args} is non-empty a directory read bypasses the parquet
cache, whose readers take no options.}

\item{...}{Forwarded to \code{\link[blockr.core:new_data_block]{blockr.core::new_data_block()}}}
}
\value{
A blockr data block that reads files and returns a dm object.
}
\description{
A block for reading multiple data files into a dm (data model) object.
Supports reading from Excel files (each sheet becomes a table), ZIP archives
(containing multiple data files), directories (reading all data files), or
serialization formats (RDS, QS, RData).
}
\details{
\subsection{File Handling}{

\strong{Excel files (.xlsx, .xls):}
\itemize{
\item Each sheet becomes a table in the dm object
\item Table names are derived from sheet names
}

\strong{ZIP files (.zip):}
\itemize{
\item Archive is extracted to a temporary directory
\item All recognized data files (CSV, Excel, Parquet, etc.) are read
\item Table names are derived from file names (without extension)
}

\strong{Directories:}
\itemize{
\item All recognized data files in the directory are read
\item Subdirectories are not traversed (flat read)
\item Table names are derived from file names (without extension)
}

\strong{Serialized files (.rds):}
\itemize{
\item If the file contains a dm object, it is returned directly
\item If the file contains a data.frame, it is wrapped in a dm
\item If the file contains a list of data.frames, each becomes a dm table
}

\strong{RData files (.rdata, .rda):}
\itemize{
\item All data.frame objects in the file become dm tables
}
}
}
\section{External control}{

\code{path}, \code{selected_tables} and \code{args} are externally controllable (see
\code{\link[blockr.core:block_name]{blockr.core::external_ctrl_vars()}}), so a board update, the assistant or
any other controller can point the block at a different dataset with a
\code{mod} delta instead of replacing the block. This works because the block's
expression is a pure function of those two state variables, the way
\code{\link[blockr.io:read]{blockr.io::new_read_block()}}'s is: whoever writes them, the read follows
on the next flush. There is no separate confirm step that could leave the
reported path and the data on screen disagreeing.
}

\examples{
# Create a dm read block (interactive mode)
if (interactive()) {
  library(blockr.core)
  library(blockr.dm)
  serve(new_dm_read_block())
}

}
