% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crossfilter-backends.R
\name{crossfilter_cat_condition}
\alias{crossfilter_cat_condition}
\title{Build the filter condition for one categorical dimension}
\usage{
crossfilter_cat_condition(dim, val, col = NULL)
}
\arguments{
\item{dim}{Column name}

\item{val}{Selected levels, as a list or character vector of label strings}

\item{col}{The column itself, used to cast the literals back to its type;
\code{NULL} when the column is not available}
}
\value{
A call, or \code{NULL} when nothing is selected
}
\description{
The JS UI publishes selected levels as label STRINGS inside a JSON array,
and Shiny decodes incoming messages with \code{simplifyVector = FALSE}, so
\verb{["F"]} reaches R as \code{list("F")} — never an atomic vector. Pasting that
straight into a call produced \code{SEX == list("F")}: base R coerces the list
when the condition is evaluated on a plain data frame, so it looked like it
worked, but the generated code is wrong to read and a lazy (DuckDB/dbplyr)
backend cannot translate it at all.
}
\details{
The sentinels get the same treatment: \code{CROSSFILTER_NA} means "the missings",
which is \code{is.na(col)}, not a literal \code{"__NA__"} that matches nothing — the
same reading \code{\link[=apply_crossfilter_filters]{apply_crossfilter_filters()}} uses for the bar counts, so the
expression mirrors the numbers the UI shows.
}
\keyword{internal}
