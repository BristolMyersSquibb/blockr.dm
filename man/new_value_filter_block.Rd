% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/value-filter-block.R
\name{new_value_filter_block}
\alias{new_value_filter_block}
\title{Value filter block}
\usage{
new_value_filter_block(state = list(columns = list()), ...)
}
\arguments{
\item{state}{List with \code{columns} — a list of column-object entries — and
\code{operator} ("&" or "|", default "&") controlling how the per-column
conditions combine on a data-frame input (same AND/OR pill as the
blockr.dplyr filter block; a builder option, so it sits in the gear
popover and shows once two or more fields are active). On a \code{dm} input
the operator is ignored: the restriction cascades per-table through
\code{dm::dm_filter()}, whose chaining is inherently AND, so the pill is not
offered there. Each column entry has \code{name} (column name), \code{table}
(source table; only when input is a \code{dm}), \code{mode} (\code{"single"} or
\code{"multi"}), and \code{values} (character vector of selected values). Logical
columns in single mode render as a switch instead of a dropdown: ON
filters to \code{col \%in\% TRUE}, OFF to \code{!(col \%in\% TRUE)} — i.e. missings
count as FALSE. For NA as an explicit category, flip the column to
Multi (the \verb{<NA>} token) or use the blockr.dplyr filter block.
Old-style state (\code{list(columns=character, modes=list, values=list)})
is auto-migrated via \code{\link[=migrate_value_filter_state]{migrate_value_filter_state()}} for backward
compatibility.}

\item{...}{Additional arguments forwarded to \code{\link[blockr.core:new_transform_block]{blockr.core::new_transform_block()}}.}
}
\description{
Minimal value filter for data frames or \code{dm} objects. The columns to filter
on are chosen behind the gear icon (top-right of the block). Each column
can be toggled between single-select (always constrains — auto-picks first
value) and multi-select (empty selection passes through).
}
\details{
When the upstream input is a \code{dm}, the gear popover gains a table selector
so columns can be picked from any of the dm's tables. The emitted filter
goes through \code{dm::dm_filter()}, so the restriction cascades through foreign
keys to related tables.

Gear/popover UX, select widget, and click-through pill styling are reused
from \code{blockr.dplyr} to match the look-and-feel of the crossfilter and dplyr
transform blocks.

This is the value-selection member of the relational filter family,
alongside \code{\link[=new_dm_filter_block]{new_dm_filter_block()}} (expression-style) and
\code{\link[=new_crossfilter_block]{new_crossfilter_block()}}. It lives in \code{blockr.dm} because its \code{dm}
path depends on \code{dm}; it is fully usable in a dashboard by composing it
onto a board.
}
\examples{
if (interactive()) {
  library(blockr.core)
  library(blockr.dm)
  serve(
    new_value_filter_block(
      state = list(
        columns = list(
          list(name = "Species", mode = "single", values = "setosa")
        )
      )
    ),
    data = list(data = iris)
  )
}

}
