% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dm-semi-filter-block.R
\name{new_dm_semi_filter_block}
\alias{new_dm_semi_filter_block}
\title{dm Semi-Filter Block Constructor}
\usage{
new_dm_semi_filter_block(
  table = "adsl",
  key_col = "USUBJID",
  distinct_only = TRUE,
  ...
)
}
\arguments{
\item{table}{Character. Name of the dm table to filter. Default
\code{"adsl"}.}

\item{key_col}{Character. Column name used for the semi-join. Must
exist in both \code{ids} and in \code{table}. Default \code{"USUBJID"}.}

\item{distinct_only}{Logical. \code{TRUE} (default) uses \code{unique()} of
\code{ids[[key_col]]} before matching. Set \code{FALSE} to pass the raw
column through.}

\item{...}{Forwarded to \code{\link[blockr.core:new_transform_block]{blockr.core::new_transform_block()}}.}
}
\value{
A block object for semi-filtering a dm by ids from a
secondary data frame.
}
\description{
Restricts a \code{dm} to rows whose \code{key_col} values appear in a second
\code{data.frame} input (\code{ids}). Equivalent to a semi-join on one chosen
table of the dm; because \code{dm::dm_filter()} cascades through foreign
keys, the restriction propagates to every related table.
}
\details{
Useful for bridging drill-down outputs (which produce data frames)
back into a dm: click a patient on a trajectory chart, feed the
resulting data frame in via \code{ids}, and downstream dm consumers
(patient profile, flatten, summaries) all see the restricted dm.
}
\examples{
# Typical wiring: a drill-down chart selects a USUBJID, which
# restricts the whole dm to that patient for a patient-profile view.
new_dm_semi_filter_block(table = "adsl", key_col = "USUBJID")

}
