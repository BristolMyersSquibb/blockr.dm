% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dm-filter-by-data-block.R
\name{new_dm_filter_by_data_block}
\alias{new_dm_filter_by_data_block}
\title{dm Filter-by-Data Block Constructor}
\usage{
new_dm_filter_by_data_block(
  table = "adsl",
  key_col = "USUBJID",
  distinct_only = TRUE,
  ...
)
}
\arguments{
\item{table}{Character. Name of the dm table to filter. Default
\code{"adsl"}.}

\item{key_col}{Character. Column name used for the match. Must
exist in both \code{by} and \code{table}. Default \code{"USUBJID"}.}

\item{distinct_only}{Logical. \code{TRUE} (default) uses \code{unique()} of
\code{by[[key_col]]} before matching. Set \code{FALSE} to pass the raw
column through.}

\item{...}{Forwarded to \code{\link[blockr.core:new_transform_block]{blockr.core::new_transform_block()}}.}
}
\value{
A block object for filtering a dm by a secondary data
frame.
}
\description{
Filters a \code{dm} by matching rows in a second data frame input (\code{by}).
Equivalent to a one-column semi-join on a chosen table of the dm;
because \code{dm::dm_filter()} cascades through foreign keys, the
restriction propagates to every related table.
}
\details{
Useful for bridging drill-down outputs (which produce data frames)
back into a dm: click a patient on a chart, feed the resulting data
frame in via \code{by}, and downstream dm consumers (patient profile,
flatten, summaries) all see the restricted dm.
}
\examples{
# Typical wiring: a drill-down chart selects a USUBJID, which
# restricts the whole dm to that patient for a patient-profile view.
new_dm_filter_by_data_block(table = "adsl", key_col = "USUBJID")

}
