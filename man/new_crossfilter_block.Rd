% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crossfilter-block.R
\name{new_crossfilter_block}
\alias{new_crossfilter_block}
\title{Crossfilter Block}
\usage{
new_crossfilter_block(
  active_dims = list(),
  filters = list(),
  range_filters = list(),
  measure = NULL,
  agg_func = NULL,
  featured = character(),
  pinned = NULL,
  groups = list(),
  subgroup = NULL,
  ...
)
}
\arguments{
\item{active_dims}{Named list of per-table active filter columns.
E.g., \code{list(adsl = c("SEX", "AGE"), adae = c("AESEV"))}}

\item{filters}{Named list of per-table categorical filters.
E.g., \code{list(adae = list(AESEV = c("SEVERE")))}}

\item{range_filters}{Named list of per-table range filters.
E.g., \code{list(adsl = list(AGE = c(40, 80)))}}

\item{measure}{Measure column as \code{"table.column"} string, or \code{NULL}
for row counts. E.g., \code{"orders.amount"}.}

\item{agg_func}{Aggregation function: \code{"sum"} or \code{"mean"}. Only used
when \code{measure} is set.}

\item{featured}{Columns worth showing up front: they get a pill each above
the cards, rank first in the search, and are the columns \code{pinned} may
name. A pill's one job is to open and close that column's filter card.
E.g. \code{c("SEX", "RACE", "AGE")}. Empty (the default) means no pills, but
the row is still there: it carries \verb{+ More filters}, which opens the
column search and is the only way in once blockr.dock has hidden the gear
on a simplified or locked board. The vocabulary itself is edited in the
gear, as a \verb{Shown up front} field beside \code{Measure} and \code{Aggregation}:
dropping a column from it also takes it out of \verb{Group by}, for every
reader of the board, which is a setting and not a filter.}

\item{pinned}{The single column the block reports as the board's group,
chosen in the \verb{Group by} select at the top of the block. Only \code{featured}
columns on the parent table qualify. Grouping is independent of filtering: a pinned
column need not have a card, and a card does not make a column the group.
\code{NULL} (the default) means no group.}

\item{groups}{Group definitions for the pinned column and the subgroup,
edited in the \code{Pools} section under each field. A named list keyed by
column, so switching the group column and back, or swapping group and
subgroup, keeps each definition. An entry is
\verb{list(show = <levels with a column of their own, in order>, pools = list(list(name = , members = , custom = )))}, \code{custom = FALSE} meaning the
pool's name follows its members. A column with no entry shows every level
in \code{\link[=crossfilter_level_order]{crossfilter_level_order()}} and has no pools. The block only records
the definition; a package building on it (blockr.pharma's population
filter) applies it.}

\item{subgroup}{A second column the board is split by, under \code{pinned},
chosen in the \verb{Subgroup by} select below the groups. Same eligible
columns as \code{pinned}, minus the pinned one. \code{NULL} (the default) means no
subgroup. Its levels are pooled like the group's, through its entry in
\code{groups}, and a button in its field swaps it with \code{pinned}. Like
\code{pinned}, the block only records it; blockr.pharma's population filter
stamps it as \code{Subgroup}.}

\item{...}{Forwarded to \code{\link[blockr.core:new_transform_block]{blockr.core::new_transform_block()}}. A package
building on this block passes its own \code{class} here: the subclass has to be
set at construction, which is where block metadata is resolved from the
registry. It is not a formal because every constructor formal has to come
back out as block state.}
}
\value{
A blockr transform block with client-side crossfiltering
}
\description{
Client-side crossfilter using crossfilter2.js. R builds lookup tables and
ships them to the browser; all filtering, counting, and UI updates happen
in JavaScript for instant responsiveness. Accepts either a data frame or
a \link[dm:dm]{dm} object.
}
