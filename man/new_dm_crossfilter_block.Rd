% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crossfilter-block.R
\name{new_dm_crossfilter_block}
\alias{new_dm_crossfilter_block}
\title{Legacy dm-only crossfilter constructor (deprecated)}
\usage{
new_dm_crossfilter_block(
  active_dims = list(),
  filters = list(),
  range_filters = list(),
  measure = NULL,
  agg_func = NULL,
  ...
)
}
\arguments{
\item{active_dims}{Named list of per-table active filter columns.
E.g., \code{list(adsl = c("SEX", "AGE"), adae = c("AESEV"))}}

\item{filters}{Named list of per-table categorical filters.
E.g., \code{list(adae = list(AESEV = c("SEVERE")))}}

\item{range_filters}{Named list of per-table range filters.
E.g., \code{list(adsl = list(AGE = c(40, 80)))}}

\item{measure}{Measure column as \code{"table.column"} string, or \code{NULL}
for row counts. E.g., \code{"orders.amount"}.}

\item{agg_func}{Aggregation function: \code{"sum"} or \code{"mean"}. Only used
when \code{measure} is set.}

\item{...}{Forwarded to \code{\link[blockr.core:new_transform_block]{blockr.core::new_transform_block()}}}
}
\value{
A crossfilter block carrying a \code{dm_crossfilter_block} subclass.
}
\description{
\code{new_dm_crossfilter_block()} is the pre-unification alias kept so saved
boards that reference the \code{dm_crossfilter_block} UID continue to
deserialize. The block now delegates to \code{\link[=new_crossfilter_block]{new_crossfilter_block()}} —
which accepts both data frames and dm objects — and emits a
\code{\link[=.Deprecated]{.Deprecated()}} warning at construction time. Produced blocks carry
a \code{dm_crossfilter_block} subclass so the served Shiny UI renders a
visible deprecation banner above the normal block output.
}
