% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dm-crossfilter-block.R
\name{new_dm_crossfilter_block}
\alias{new_dm_crossfilter_block}
\title{dm Crossfilter Block}
\usage{
new_dm_crossfilter_block(
  active_dims = list(),
  filters = list(),
  range_filters = list(),
  ...
)
}
\arguments{
\item{active_dims}{Named list of per-table active filter columns. Each element
is a character vector of column names to show as filter widgets.
E.g., \code{list(adsl_data = c("SEX", "AGE"), adlb_data = c("PARAMCD"))}
Start with an empty list (default) to show no filters initially.}

\item{filters}{Named list of per-table categorical filters. Each element is
itself a named list of character vectors.
E.g., \code{list(adsl = list(SEX = c("F")), adae = list(AESEV = c("SEVERE")))}}

\item{range_filters}{Named list of per-table range filters. Each element is
a named list of numeric(2) vectors.
E.g., \code{list(adsl = list(AGE = c(65, 80)))}}

\item{...}{Forwarded to \code{\link[blockr.core:new_transform_block]{blockr.core::new_transform_block()}}}
}
\value{
A blockr transform block that returns a filtered dm object
}
\description{
A crossfilter block that accepts a dm object, shows per-table filter panels,
and propagates filters across tables using the dm's key relationships.
For example, filtering AESEV=SEVERE in ADAE reduces the subject set visible
in ADLB and ADSL panels.
}
