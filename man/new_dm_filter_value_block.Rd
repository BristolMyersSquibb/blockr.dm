% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dm-filter-value-block.R
\name{new_dm_filter_value_block}
\alias{new_dm_filter_value_block}
\title{dm Filter Value Block Constructor}
\usage{
new_dm_filter_value_block(table = "", column = "", value = "", ...)
}
\arguments{
\item{table}{Character, the name of the table to filter. Default "".}

\item{column}{Character, the column to filter on. Default "".}

\item{value}{Character, the value to filter for. Default "".}

\item{...}{Forwarded to \code{\link[blockr.core:new_transform_block]{blockr.core::new_transform_block()}}}
}
\value{
A block object for filtering dm objects by value
}
\description{
This block filters a dm object by selecting a table, column, and value from
dropdown menus. The filter cascades to related tables via semi-joins based on
foreign key relationships. This is a point-and-click alternative to
\code{\link[=new_dm_filter_block]{new_dm_filter_block()}} which requires free-text expressions.
}
\details{
Three cascading dropdowns let the user pick a table, then a column within
that table, then a specific value from that column. The resulting filter is
equivalent to \verb{dm::dm_filter(dm, <table> = <column> == <value>)}.

When the dm has foreign key relationships defined, filtering one table
automatically filters related tables to only include matching rows.
}
\examples{
new_dm_filter_value_block(table = "adsl", column = "SEX", value = "F")

}
