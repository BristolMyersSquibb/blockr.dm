% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crossfilter-backends.R
\name{compress_crossfilter_lookups}
\alias{compress_crossfilter_lookups}
\title{Compress the encoded lookups for the wire}
\usage{
compress_crossfilter_lookups(encoded_lookups)
}
\arguments{
\item{encoded_lookups}{The \code{lookups} element of
\code{\link[=encode_crossfilter_payload]{encode_crossfilter_payload()}}'s result (named list of \code{json} strings)}
}
\value{
Named list of single-line base64 strings of deflated JSON
}
\description{
httpuv does not negotiate websocket permessage-deflate (verified against
the handshake: the 101 response carries no Sec-WebSocket-Extensions), so
Shiny custom messages travel uncompressed. Deflating the columnar JSON in
R and inflating with the browser's native DecompressionStream recovers
what the transport won't give us; the dictionary-encoded int arrays
compress extremely well, far outweighing base64's 4/3 inflation.
}
\details{
Format trap: R's \code{memCompress(type = "gzip")} emits ZLIB framing
(RFC 1950, \verb{0x78 0x9C} header), NOT gzip (RFC 1952, \verb{0x1F 0x8B}). The
Compression Streams API calls that format \code{'deflate'}, so the payload's
\code{compression} field says "deflate" and the client inflates with
\code{DecompressionStream('deflate')} -- \code{'gzip'} there fails on the header.

Gate: \code{options(blockr.dm.crossfilter_gzip = FALSE)} ships plain JSON for
clients without DecompressionStream (Safari < 16.4); the client accepts
both forms per message, keyed on the payload's \code{compression} field.
}
\keyword{internal}
