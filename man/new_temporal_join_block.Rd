% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temporal-join-block.R
\name{new_temporal_join_block}
\alias{new_temporal_join_block}
\title{Temporal Join Block}
\usage{
new_temporal_join_block(
  by = character(),
  left_date = character(),
  right_date = character(),
  window_days = 7,
  direction = "after",
  ...
)
}
\arguments{
\item{by}{Character. Column to join on (e.g., "USUBJID"). If empty,
auto-detected from common columns.}

\item{left_date}{Character. Date column in x (left table).}

\item{right_date}{Character. Date column in y (right table).}

\item{window_days}{Numeric. Time window in days. Default 7.}

\item{direction}{Character. One of "after", "before", or "around".
"after": right_date 0 to window_days after left_date.
"before": right_date 0 to window_days before left_date.
"around": right_date within window_days in either direction.}

\item{...}{Forwarded to \code{\link[blockr.core:new_transform_block]{blockr.core::new_transform_block()}}}
}
\value{
A transform block that takes two data frame inputs (x, y) and
returns a joined data frame with a \code{days_diff} column.
}
\description{
A standalone temporal join block that takes two data frames as inputs,
joins on a key column, computes time difference between date columns,
and filters to a time window. Designed for clinical trial data where
you need to find events within a time window (e.g., labs within 7 days
of an adverse event for causality assessment).
}
\examples{
# Find labs within 7 days after each adverse event
new_temporal_join_block(
  by = "USUBJID",
  left_date = "ASTDT",
  right_date = "ADT",
  window_days = 7,
  direction = "after"
)

}
