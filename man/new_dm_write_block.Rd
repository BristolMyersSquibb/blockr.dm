% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dm-write-block.R
\name{new_dm_write_block}
\alias{new_dm_write_block}
\title{Write dm object to files}
\usage{
new_dm_write_block(
  directory = "",
  filename = "",
  format = "excel",
  mode = "download",
  auto_write = FALSE,
  ...
)
}
\arguments{
\item{directory}{Character. Default directory for file output (browse mode only).
Can be configured via \code{options(blockr.write_dir = "/path")}. Default: \code{tempdir()}.}

\item{filename}{Character. Optional fixed filename (without extension).
\itemize{
\item \strong{If provided}: Writes to same file path on every upstream change
\item \strong{If empty} (default): Generates timestamped filename
}}

\item{format}{Character. Output format: "excel", "csv", or "parquet".
Default: "excel" (best for multi-table dm objects).}

\item{mode}{Character. Either "download" (triggers browser download),
or "browse" (writes to server filesystem). Default: "download"}

\item{auto_write}{Logical. When TRUE, automatically writes files when data changes
(browse mode only). Default: FALSE.}

\item{...}{Forwarded to \code{\link[blockr.core:new_transform_block]{blockr.core::new_transform_block()}}}
}
\value{
A blockr transform block that writes dm objects to files
}
\description{
A block for writing dm (data model) objects to files. Supports writing to
Excel files (each table as a sheet), ZIP archives (containing multiple files),
or directories (one file per table).
}
\details{
\subsection{Output Formats}{

\strong{Excel (.xlsx):}
\itemize{
\item Single Excel file with multiple sheets
\item Each dm table becomes a sheet
\item Sheet names derived from table names
}

\strong{CSV/Parquet (in ZIP):}
\itemize{
\item Single ZIP file containing individual files
\item Each dm table becomes a separate file
\item Filenames derived from table names
}

\strong{Directory:}
\itemize{
\item Only available in browse mode
\item Each dm table written as separate file
\item Files named from table names
}
}
}
\examples{
if (interactive()) {
  library(blockr.core)
  library(blockr.dm)
  serve(new_dm_write_block())
}

}
