% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dm-block.R
\name{dm_block_shape}
\alias{dm_block_shape}
\title{Everything the dm-block expression needs to know about its inputs}
\usage{
dm_block_shape(refs, vals, do_infer)
}
\arguments{
\item{refs}{Named character vector from \code{dot_arg_refs()}: eval-env symbols
keyed by display (link) name.}

\item{vals}{List of realized slot values, in slot order.}

\item{do_infer}{Logical, whether to infer PK/FK relationships.}
}
\value{
A list with \code{nms} (eval-env symbols), \code{display_nms}, \code{is_dm},
\code{pks} and \code{fks}
}
\description{
Reduces the realized variadic inputs to the plain, comparable description
the expression builder reads: the eval-env symbol and display name per
slot, the dm-vs-data-frame classification, and the inferred key
relationships. Deliberately returns base vectors and lists (never the
input data itself) so two equal-but-freshly-pulled inputs produce an
\code{identical()} result — that is what lets the \code{reactiveVal} holding it
treat a spurious re-derivation as a no-op. See the note on the observer
in \code{\link[=new_dm_block]{new_dm_block()}}.
}
\keyword{internal}
