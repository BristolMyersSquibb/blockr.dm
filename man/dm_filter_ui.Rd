% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dm-filter-module.R
\name{dm_filter_ui}
\alias{dm_filter_ui}
\alias{dm_filter_server}
\title{Standalone Crossfilter Module for dm Objects}
\usage{
dm_filter_ui(id)

dm_filter_server(
  id,
  dm,
  active_dims = list(),
  filters = list(),
  range_filters = list(),
  measure = NULL
)
}
\arguments{
\item{id}{Character string. The Shiny module namespace ID.}

\item{dm}{A reactive or \code{\link[shiny:reactiveVal]{shiny::reactiveVal()}} returning a \code{\link[dm:dm]{dm::dm()}} object.
The dm should have primary and foreign keys defined for cross-table
filtering to work.}

\item{active_dims}{Named list of per-table active filter columns. Each
element is a character vector of column names to show as initial filter
widgets. E.g., \code{list(adsl = c("SEX", "AGE"), adae = c("AESEV"))}.
Defaults to an empty list (no filters shown initially; users add via
search).}

\item{filters}{Named list of per-table categorical filters (initial state).
E.g., \code{list(adsl = list(SEX = c("F")))}.}

\item{range_filters}{Named list of per-table range filters (initial state).
E.g., \code{list(adsl = list(AGE = c(65, 80)))}.}

\item{measure}{Measure column for aggregation, as \code{"table.column"}
(e.g., \code{"orders.amount"}). When set, categorical filter bars show
\code{SUM(column)} instead of row counts. Defaults to \code{NULL} (row count).}
}
\value{
\code{dm_filter_ui} returns a Shiny UI tag list containing the
crossfilter search bar, filter widgets, and status display.

\code{dm_filter_server} returns a reactive that yields the filtered
\code{\link[dm:dm]{dm::dm()}} object. When no filters are active, the original dm is returned
unchanged.
}
\description{
A standard Shiny module pair that provides interactive crossfilter UI for
\link[dm:dm]{dm} objects. Unlike \code{\link[=new_dm_crossfilter_block]{new_dm_crossfilter_block()}}, this module can
be embedded in any Shiny app without requiring blockr.core, blockr.dag, or
the blockr board infrastructure.
}
\examples{
if (interactive()) {
  library(shiny)
  library(dm)

  orders <- data.frame(
    id = 1:100,
    region = sample(
      c("North", "South", "East", "West"), 100,
      replace = TRUE
    ),
    amount = runif(100, 10, 1000)
  )
  items <- data.frame(
    order_id = sample(1:100, 200, replace = TRUE),
    product = sample(c("A", "B", "C"), 200, replace = TRUE)
  )
  my_dm <- dm(orders = orders, items = items) |>
    dm_add_pk(orders, id) |>
    dm_add_fk(items, order_id, orders)

  ui <- fluidPage(
    column(4, dm_filter_ui("cf")),
    column(8, tableOutput("tbl"))
  )

  server <- function(input, output, session) {
    filtered <- dm_filter_server("cf", reactiveVal(my_dm),
      active_dims = list(orders = c("region"), items = c("product"))
    )
    output$tbl <- renderTable(filtered()[["orders"]])
  }

  shinyApp(ui, server)
}

}
\seealso{
\code{\link[=new_dm_crossfilter_block]{new_dm_crossfilter_block()}} for the blockr-integrated version.
}
